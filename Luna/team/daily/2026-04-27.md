# Daily Log — 2026-04-27

## 📋 Task Summary

**Issue:** FirebaseError เมื่อกดเพิ่มลูกค้าใหม่ในหน้า quotations/new

**Error Message:**
```
FirebaseError: Function addDoc() called with invalid data. Unsupported field value: undefined (found in field email in document customers/sPMd9a5fZKFH0Dso2TM9)
```

---

## 👥 Team

**Role:** Stella (Frontend Dev)
**Reason:** Frontend bug - React component + Firebase integration

---

## 🔍 Investigation

### Root Cause
- `CustomerPicker.tsx` บรรทัด 215: set `email` เป็น `undefined` เมื่อเป็น empty string
- Firestore ไม่รองรับค่า `undefined` ใน document fields โดยตรง
- เกิด error เมื่อ `addDoc()` พยายามบันทึก document ที่มี field `email: undefined`

### Files Analyzed
- `d:\coding\UniformFlow\src\lib\customers.ts` — createCustomer function
- `d:\coding\UniformFlow\src\components\admin\CustomerPicker.tsx` — modal สำหรับเพิ่ม/แก้ลูกค้า
- `d:\coding\UniformFlow\src\components\admin\QuotationForm.tsx` — form ที่เรียกใช้ CustomerPicker

---

## ✅ Solution

**Approach:** Sanitize data ก่อนส่งไป Firestore โดย filter undefined values ออก

**Implementation:**
- แก้ใน `src/lib/customers.ts` function `createCustomer`
- เพิ่ม `sanitizedData` ที่ filter undefined values:
  ```typescript
  const sanitizedData = Object.fromEntries(
    Object.entries(data).filter(([_, v]) => v !== undefined)
  );
  ```

**Benefits:**
- แก้ปัญหาที่ source (createCustomer function)
- ป้องกันปัญหาเดียวกันจากที่อื่นที่เรียก createCustomer
- เป็น best practice ในการ sanitize data ก่อนส่ง database

---

## 📝 Files Changed

- `d:\coding\UniformFlow\src\lib\customers.ts` (lines 70-83)

---

## 🧪 Testing

**Status:** รอการทดสอบจากผู้ใช้
- พี่แมนควรลองกดเพิ่มลูกค้าใหม่ใหม่ที่ `http://localhost:3000/dashboard/quotations/new`

---

## 🌙 Luna Review

การแก้ไขของสเตลลาถูกต้อง ✓
- แก้ที่ source ของปัญหา
- ใช้วิธี sanitize data ก่อนส่ง database (best practice)
- ป้องกันปัญหาเดียวกันจากที่อื่น

---

## 📊 Metrics

| Metric | Value |
|--------|-------|
| Files changed | 1 |
| Lines changed | ~5 |
| TypeScript errors | 0 |
| Role used | Stella (Frontend Dev) |

---

## 🚀 Task 2: Firebase Deployment

**Request:** Deploy ขึ้น Firebase

**Role:** Cosmos (DevOps Engineer)
**Reason:** DevOps/Deployment task

### Investigation
- เช็ค `firebase.json` config - พบว่าใช้ Firebase Frameworks Backend
- Firebase Frameworks Backend support Next.js SSR โดยตรง (ไม่ต้อง export static)
- Config: `frameworksBackend: { region: "asia-east1" }`

### Deployment Process
- เช็ค Firebase CLI version: 15.15.0 ✓
- รัน `firebase deploy`
- Firebase CLI ทำ:
  1. Build Next.js app อัตโนมัติ
  2. Deploy Hosting (111 files)
  3. Deploy Functions (Node.js 24 2nd Gen, asia-east1)
  4. Deploy Firestore rules + indexes
  5. Deploy Storage rules

### Result
✓ **Deploy สำเร็จ**

**URLs:**
- Hosting: https://uniformflow-ee6df.web.app
- Console: https://console.firebase.google.com/project/uniformflow-ee6df/overview
- Function URL: https://ssruniformflowee6df-jjmid2ywxq-de.a.run.app

**Warnings:**
- firebase-functions version outdated (non-blocking)

### Luna Review
การ deploy ของคอสมอสถูกต้อง ✓
- ใช้ Firebase Frameworks Backend ที่เหมาะสมกับ Next.js SSR
- Deploy ครบทุก component (Hosting, Functions, Firestore, Storage)
- Region asia-east1 เหมาะสมกับผู้ใช้ในไทย

### Additional Metrics

| Metric | Value |
|--------|-------|
| Deployment method | Firebase Frameworks Backend |
| Functions runtime | Node.js 24 2nd Gen |
| Functions region | asia-east1 |
| Hosting files | 111 files |
| Role used | Cosmos (DevOps Engineer) |

---

## 🚀 Task 3: เพิ่มฟิลด์ "เขต/อำเภอ" ในที่อยู่ลูกค้า

**Request:** เพิ่มฟิลด์ "เขต" ในส่วนที่อยู่ของหน้าเพิ่มลูกค้าใหม่ ที่ https://uniformflow-ee6df.web.app/dashboard/quotations/new

**Role:** Stella (Frontend Dev)
**Reason:** Frontend feature - UI form modification

### Investigation
- ตรวจสอบ Address type ใน `src/types/index.ts` - มีฟิลด์: street, district, province, postcode, fullAddress
- ปัจจุบัน `district` placeholder เป็น "แขวง" แต่ไม่มีฟิลด์สำหรับ "เขต/อำเภอ"
- ในระบบที่อยู่ไทยที่ถูกต้องควรมี: แขวง/ตำบล + เขต/อำเภอ + จังหวัด
- พี่แมนเลือกใช้มาตรฐานแบบ Lazada (แยกฟิลด์ แขวง/ตำบล และ เขต/อำเภอ)

### Solution
**Approach:** เพิ่มฟิลด์ `subdistrict` (เขต/อำเภอ) เข้าไปใน Address type

**Implementation:**
- เพิ่มฟิลด์ `subdistrict: string` ใน Address interface
- เปลี่ยน placeholder `district` → "แขวง/ตำบล"
- เพิ่ม input field ใหม่สำหรับ `subdistrict` ใน CustomerPicker
- อัปเดต default address ในทุกที่ที่สร้าง Address object
- อัปเดต formatAddressLine ให้รวม subdistrict

### Files Changed
- `d:\coding\UniformFlow\src\types\index.ts` - เพิ่ม subdistrict ใน Address interface
- `d:\coding\UniformFlow\src\lib\customers.ts` - อัปเดต default address
- `d:\coding\UniformFlow\src\lib\company.ts` - อัปเดต emptyAddress และ formatAddressLine
- `d:\coding\UniformFlow\src\components\admin\CustomerPicker.tsx` - เพิ่ม input field และปรับ placeholder
- `d:\coding\UniformFlow\src\components\admin\DeliveryNoteForm.tsx` - อัปเดต emptyAddress
- `d:\coding\UniformFlow\src\app\dashboard\company\page.tsx` - เพิ่ม input field ในฟอร์มบริษัท
- `d:\coding\UniformFlow\src\lib\customer-import.ts` - อัปเดต address object

### Luna Review
การแก้ไขของสเตลลาถูกต้อง ✓
- เพิ่มฟิลด์ใหม่โดยไม่กระทบข้อมูลเก่า (backward compatible)
- อัปเดตทุกจุดที่สร้าง Address object
- อัปเดต formatAddressLine ให้แสดงผลถูกต้องใน PDF
- ตรวจสอบและอัปเดตทุกไฟล์ที่ใช้ Address type

### Metrics

| Metric | Value |
|--------|-------|
| Files changed | 7 |
| New field added | subdistrict (เขต/อำเภอ) |
| TypeScript errors | 0 |
| Role used | Stella (Frontend Dev) |

---

## 🚀 Task 4: แก้ปัญหา Firestore Index Error

**Request:** FirebaseError เมื่อ query ข้อมูลจาก Firestore - "The query requires an index"

**Role:** Nebula (Database Designer)
**Reason:** Database indexing task

### Investigation
- Error message บอกว่า query ต้องการ composite index
- ตรวจสอบ firestore.indexes.json และ queries ทั้งหมด
- พบว่า indexes ที่ขาด:
  1. **quotations** - `where("status", "==", opts.status)` + `orderBy("createdAt", "desc")`
     - ปัจจุบันมี: status + issueDate (ผิด field)
  2. **invoices** - `where("status", "==", opts.status)` + `orderBy("issueDate", "desc")`
     - ปัจจุบันมี: status + dueDate (ผิด field)

### Solution
**Approach:** แก้ไข firestore.indexes.json ให้ตรงกับ query patterns ที่ใช้จริง

**Implementation:**
- เปลี่ยน quotations index: status + issueDate → status + createdAt
- เพิ่ม invoices index: status + issueDate
- Deploy indexes ไป Firestore โดยใช้ `firebase deploy --only firestore:indexes`

### Files Changed
- `d:\coding\UniformFlow\firebase\firestore.indexes.json`

### Deployment Result
✓ **Deploy สำเร็จ**
- Firebase ลบ index เก่าที่ไม่ใช้แล้ว (quotations: status + issueDate)
- Deploy indexes ใหม่สำเร็จ

### Luna Review
การแก้ไขของเนบิวลาถูกต้อง ✓
- วิเคราะห์ query patterns ทั้งหมด
- สร้าง composite indexes ที่ตรงกับการใช้งานจริง
- Deploy และลบ indexes ที่ไม่ใช้แล้วอย่างถูกต้อง

### Metrics

| Metric | Value |
|--------|-------|
| Files changed | 1 |
| Indexes added | 2 |
| Indexes deleted | 1 |
| Role used | Nebula (Database Designer) |
