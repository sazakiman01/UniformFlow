# Daily Log - 2026-05-05

## สิ่งที่ทำวันนี้

### ปรับ Contrast หน้า Business Model Canvas ให้อ่านง่ายขึ้น

**ปัญหา:**
- หน้า `/dashboard/admin/business-model` ดูกลืนกับพื้นหลัง
- อ่านแล้วสว่างแสบตาเกินไป
- Background cards ใช้สี pastel จางมาก (opacity /40) กลืนกับ `bg-slate-50/50`
- Border colors จางเกินไป (blue-100, emerald-100 ฯลฯ)
- Text colors มี contrast ต่ำกับ background ที่จาง

**วิธีแก้ (Hybrid Approach):**
1. **เพิ่ม opacity background cards** จาก `/40` → `/80` (ชัดขึ้นแต่ยังคงสี pastel)
2. **เพิ่มความเข้ม border** จาก `100` → `200` (ชัดขึ้น)
3. **เพิ่ม contrast text colors:**
   - TONES object: text จาก `700` → `800`, iconBg จาก `100` → `200`
   - BlockCard: list items `slate-700` → `slate-800`, empty state `slate-400` → `slate-500`
   - FinanceBlockCard: item names `slate-800` → `slate-900`, amounts `slate-900` → `slate-950`
4. **เพิ่ม subtle shadow:** เพิ่ม `shadow-sm` เป็น base class และ `hover:shadow-md` สำหรับทั้ง BlockCard และ FinanceBlockCard

**ผลลัพธ์:**
- Cards ชัดขึ้น ไม่กลืนกับ background
- Border เห็นชัดเจนขึ้น
- Text อ่านง่ายขึ้น ไม่ต้องจ้องมอง
- ยังคงสีสันที่สวยงาม (ไม่เปลี่ยนเป็นขาวล้วน)
- เปลี่ยนแปลงน้อยที่สุด ไม่กระทบ design มาก

**ไฟล์ที่แก้ไข:**
- d:\coding\UniformFlow\src\app\dashboard\admin\business-model\page.tsx

**Role:** Stella (Frontend Dev) + Vega (UI/UX Designer)

---

### เพิ่ม Tooltip และตัวอย่างข้อมูลสำหรับ 9 องค์ประกอบ Business Model Canvas

**ปัญหา:**
- หน้า edit Business Model Canvas มีแค่ title + subtitle สำหรับแต่ละ block
- หน้าแสดงผล (list page) ก็ไม่มีคำอธิบายเพิ่มเติมหรือตัวอย่างข้อมูล
- มือใหม่ไม่เข้าใจว่าควรใส่ข้อมูลอะไรในแต่ละ block
- อาจทำให้บันทึกข้อมูลผิดหรือไม่ครบถ้วน

**วิธีแก้:**
1. **เพิ่ม HelpCircle icon** สำหรับ tooltip (import จาก lucide-react)
2. **เพิ่ม data structure:** 
   - หน้า edit: เพิ่ม `tooltip` และ `examples` fields ใน `stringFields` array สำหรับ 7 blocks
   - หน้าแสดงผล: เพิ่ม `BLOCK_INFO` object สำหรับ 9 blocks
3. **เพิ่ม tooltip และ examples props:** ใน FinanceBlock สำหรับ Revenue Streams และ Cost Structure (หน้า edit)
4. **อัปเดต StringBlock component:** รับ `tooltip` และ `examples` props, เพิ่ม icon คำถาม (?) ข้าง title, เพิ่มตัวอย่างใน placeholder (หน้า edit)
5. **อัปเดต FinanceBlock component:** รับ `tooltip` และ `examples` props, เพิ่ม icon คำถาม (?) ข้าง title, เพิ่มตัวอย่างใน placeholder (หน้า edit)
6. **อัปเดต BlockCard component:** รับ `tooltip` และ `examples` props, เพิ่ม icon คำถาม (?) ข้าง title (หน้าแสดงผล)
7. **อัปเดต FinanceBlockCard component:** รับ `tooltip` และ `examples` props, เพิ่ม icon คำถาม (?) ข้าง title (หน้าแสดงผล)
8. **อัปเดต JSX:** ส่ง `tooltip` และ `examples` props ในการเรียก components (ทั้งหน้า edit และหน้าแสดงผล)

**คำอธิบายและตัวอย่างข้อมูล 9 องค์ประกอบ:**
- Customer Segments: กลุ่มลูกค้าที่เราต้องการบริการ แบ่งตามลักษณะเฉพาะ (เช่น B2B SMEs, ร้านค้าออนไลน์)
- Value Propositions: สิ่งที่เรานำเสนอให้ลูกค้าที่แตกต่างจากคู่แข่ง (เช่น ผ้าคุณภาพสูง, ราคาแข่งขันได้)
- Channels: ช่องทางที่เราใช้ติดต่อและส่งมอบคุณค่าให้ลูกค้า (เช่น เว็บไซต์, LINE OA)
- Customer Relationships: ประเภทความสัมพันธ์ที่เราสร้างกับลูกค้าแต่ละกลุ่ม (เช่น Personal Assistant, Self-service)
- Revenue Streams: วิธีที่เราสร้างรายได้จากลูกค้าแต่ละกลุ่ม (เช่น ขายส่ง, ขายปลีก)
- Key Resources: ทรัพยากรที่จำเป็นต่อการทำงานของโมเดลธุรกิจ (เช่น โรงงาน, คลังสินค้า)
- Key Activities: กิจกรรมที่ต้องทำเพื่อให้โมเดลธุรกิจทำงานได้ (เช่น ผลิตผ้า, ควบคุมคุณภาพ)
- Key Partnerships: ผู้ที่เราทำงานด้วยเพื่อลดความเสี่ยงและเพิ่มประสิทธิภาพ (เช่น ซัพพลายเออร์ผ้า, โลจิสติกส์)
- Cost Structure: ต้นทุนที่จำเป็นต่อการดำเนินงานโมเดลธุรกิจ (เช่น ค่าวัตถุดิบ, ค่าแรง)

**ผลลัพธ์:**
- มือใหม่สามารถ hover ที่ icon คำถาม (?) เพื่อดูคำอธิบายและตัวอย่างข้อมูล (ทั้งหน้า edit และหน้าแสดงผล)
- Placeholder ใน input fields แสดงตัวอย่างข้อมูลที่ชัดเจน (หน้า edit)
- ช่วยให้บันทึกข้อมูลถูกต้องและครบถ้วนขึ้น
- UX ดีขึ้นสำหรับผู้ใช้ใหม่ทั้งในโหมดแก้ไขและโหมดดูข้อมูล

**ไฟล์ที่แก้ไข:**
- d:\coding\UniformFlow\src\app\dashboard\admin\business-model\[id]\page.tsx (หน้า edit)
- d:\coding\UniformFlow\src\app\dashboard\admin\business-model\page.tsx (หน้าแสดงผล)

**Role:** Vega (UI/UX Designer) + Stella (Frontend Dev)

---

### Deploy ไป Firebase (หลังเพิ่ม Tooltip และตัวอย่างข้อมูล)

**สิ่งที่ deploy:**
- Firebase Frameworks Backend (Next.js SSR)
- Firestore rules
- Firestore indexes
- Storage rules
- Hosting (140 files)
- Functions (Node.js 24 2nd Gen, region: asia-east1)

**URLs:**
- Hosting: https://uniformflow-ee6df.web.app
- Console: https://console.firebase.google.com/project/uniformflow-ee6df/overview
- Function URL: https://ssruniformflowee6df-jjmid2ywxq-de.a.run.app

**Issues ที่แก้:**
- TypeScript errors ใน `[id]/page.tsx`: เปลี่ยนจาก `undefined as any` เป็น `0` และ type ที่ถูกต้อง

**Warnings:**
- firebase-functions version outdated (non-blocking)
- npm warn EBADENGINE Unsupported engine (non-blocking)

**Role:** Cosmos (DevOps Engineer)

---

### แก้ปัญหา Mobile Cache ด้วย Service Worker

**ปัญหา:**
- ผ่านมาหลายวนแล้ว มือถือยังแสดงข้อมูลเก่า
- เปิดมา redirect ไปหน้า `https://uniformflow-ee6df.web.app/app/home` ซึ่งเป็น route เก่าที่ไม่มีในโปรเจกต์ปัจจุบัน
- ไม่มี Service Worker ในโปรเจกต์ปัจจุบัน
- ไม่มี PWA manifest ใน public folder
- Static assets cache ไว้ 1 ชั่วโมง (max-age=3600)

**สาเหตุที่เป็นไปได้:**
1. Service Worker เก่าจากเวอร์ชันก่อนหน้ายังทำงานอยู่ในมือถือ
2. PWA cache ถ้าเคยติดตั้งเป็น PWA มาก่อน
3. Firebase Hosting CDN cache
4. Mobile browser cache (iOS Safari, Chrome) แรงมาก

**วิธีแก้:**
1. **สร้าง Service Worker** (`public/sw.js`) เพื่อเคลียร์ cache เก่าอัตโนมัติเมื่อ install ใหม่
2. **สร้าง ServiceWorkerRegister component** (`src/components/ServiceWorkerRegister.tsx`) เพื่อลงทะเบียน SW
3. **อัปเดต layout.tsx** เพิ่ม ServiceWorkerRegister component
4. **ปรับ cache headers** ใน `firebase.json` เปลี่ยน max-age จาก 3600 เป็น 300 (5 นาที)
5. **Deploy ไป Firebase**

**สิ่งที่ Service Worker ทำ:**
- เคลียร์ cache เก่าทั้งหมดเมื่อ install SW ใหม่
- เคลียร์ cache ที่ไม่ตรงกับ version ปัจจุบัน
- เคลียร์ cache เก่าที่อาจ redirect ไป `/app/home`
- ไม่ cache HTML pages เพื่อให้ได้ข้อมูลล่าสุดเสมอ
- Cache static assets เฉพาะที่จำเป็น

**ผลลัพธ์:**
- Service Worker จะเคลียร์ cache เก่าอัตโนมัติเมื่อ deploy เวอร์ชันใหม่
- ไม่มีการ redirect ไป `/app/home` อีกต่อไป
- มือถือจะแสดงข้อมูลล่าสุดเสมอ
- Cache headers สั้นลงทำให้ข้อมูลอัปเดตเร็วขึ้น (5 นาที)

**ไฟล์ที่แก้ไข:**
- d:\coding\UniformFlow\public\sw.js (สร้างใหม่)
- d:\coding\UniformFlow\src\components\ServiceWorkerRegister.tsx (สร้างใหม่)
- d:\coding\UniformFlow\src\app\layout.tsx
- d:\coding\UniformFlow\firebase.json

**Role:** Cosmos (DevOps Engineer)

---

### เพิ่ม Redirect Rule สำหรับ /app/home ไป /login

**ปัญหา:**
- เปิด https://uniformflow-ee6df.web.app แล้ว redirect ไปหน้า https://uniformflow-ee6df.web.app/app/home
- ในมือถือ Chrome (OPPO) ฟ้อง page not found
- ในคอมใช้งานได้ปกติ

**วิธีแก้:**
- เพิ่ม redirect rule ใน firebase.json: `/app/home` → `/login` (301 permanent redirect)
- Deploy ไป Firebase

**ผลลัพธ์:**
- เมื่อเข้า /app/home จะ redirect ไป /login แทนที่จะฟ้อง page not found
- ช่วยแก้ปัญหา cache เก่าที่ redirect ไป route เก่า

**ไฟล์ที่แก้ไข:**
- d:\coding\UniformFlow\firebase.json

**Role:** Cosmos (DevOps Engineer)

---

## Learnings

- Contrast ที่ต่ำเกินไปทำให้ eye strain และอ่านยาก
- Hybrid approach (เพิ่ม contrast แต่ยังคงสีสัน) เป็นทางเลือกที่ดีกว่าเปลี่ยนเป็นขาวล้วน
- Shadow ช่วยแยก elements จาก background ได้ดี
- Tailwind color scale 100-200-300-400-500-600-700-800-900-950 มีความสำคัญกับ accessibility
- Confirm dialog สำคัญมากสำหรับ destructive actions (ลบข้อมูล)
- ใช้สีแดง (rose-600) สำหรับปุ่มลบเพื่อสื่อความเป็น action อันตราย
- Backend function มีอยู่แล้วแต่ไม่มี UI คือปัญหาที่พบบ่อย
- Tooltip ช่วยมือใหม่เข้าใจฟิลด์ได้ดีโดยไม่ทำให้ UI รบกวน
- ใช้ native tooltip (title attribute) เพราะ simple และไม่ต้อง install dependency เพิ่ม
- Lucide icons ไม่รองรับ title prop โดยตรง ต้อง wrap ใน span หรือ div
